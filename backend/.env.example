# Local development defaults to SQLite when DATABASE_URL is not set.
# In Render, add the Internal Database URL as DATABASE_URL through the dashboard.
DATABASE_URL=

# These are added once Google OAuth is wired. Never commit real values.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
SESSION_SECRET=
